



const containerSpace = document.getElementById("container-space")
const scheduelSpace = document.getElementById("schedule-space")



// Check local storage for existing projects, habits

// Populate interface with all projects saved in user view options

// Populate calendar

// Populate git style tracker



// Populate the page (for now, just add a default project and habit tracker)
// In the more developed version, I should check local storage and session storage for saved projects and items

function populateContainer(container, itemList) {

}

function addContainer(label, itemList) {
    let container = document.createElement()
}

// I will need to create a function that builds the card, as it is relatively complex with many sub-elements.